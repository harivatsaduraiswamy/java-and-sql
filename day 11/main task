CREATE TABLE departments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    department_name VARCHAR(100)
);

-- employees table
CREATE TABLE employees (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    department_id INT,
    hire_date DATE,
    FOREIGN KEY (department_id) REFERENCES departments(id)
);

-- salaries table
CREATE TABLE salaries (
    id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id INT,
    amount DECIMAL(10,2),
    pay_date DATE,
    FOREIGN KEY (employee_id) REFERENCES employees(id)
);
-- Departments
INSERT INTO departments (department_name) VALUES 
('Sales'), ('Engineering'), ('HR');

-- Employees
INSERT INTO employees (name, department_id, hire_date) VALUES 
('Alice', 1, '2023-02-15'),
('Bob', 2, '2022-11-01'),
('Charlie', 1, '2024-03-10');

-- Salaries
INSERT INTO salaries (employee_id, amount, pay_date) VALUES 
(1, 5000.00, '2024-01-01'),
(1, 5200.00, '2024-06-01'),
(2, 7000.00, '2024-05-01'),
(3, 4800.00, '2024-04-01');
SELECT 
  e.name, d.department_name, s.amount, s.pay_date
FROM 
  employees e
JOIN departments d ON e.department_id = d.id
JOIN salaries s ON e.id = s.employee_id;

SELECT 
  e.name, d.department_name, s.amount, s.pay_date
FROM 
  employees e
JOIN departments d ON e.department_id = d.id
JOIN salaries s ON e.id = s.employee_id
WHERE 
  d.department_name = 'Sales'
  AND e.hire_date BETWEEN '2023-01-01' AND '2024-12-31'
  AND s.pay_date = (
    SELECT MAX(pay_date) FROM salaries WHERE employee_id = e.id
);
WHERE 
  (:dept IS NULL OR d.department_name = :dept)
  AND (:start IS NULL OR e.hire_date >= :start)
  AND (:end IS NULL OR e.hire_date <= :end)














