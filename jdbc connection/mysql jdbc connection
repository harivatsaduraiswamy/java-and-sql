package jdbcconnevity;

import java.sql.*;

public class jdbcMAin {
     public static void main(String[] args) throws Exception{
    	//Class.forName("com.mysql.cj.jdbc.Driver");
    	 String Url="jdbc:mysql://localhost:3306/kgcasct";
    	 String userName="root";
    	 String Password="kgisl@123";
    	 Connection connect=DriverManager.getConnection(Url, userName, Password);
    	System.out.println("Driver Loaded");
    	
    	Statement st=connect.createStatement();
//update
String sql=("update course set cname='WD' where cid=01";
System.out.println("updated "+" "+val+"rows");
//insert datas
    String sql="create table course(Cid,cname)";
String sql="inserted into course values(01,'fsd')';
System.out.println("inserted "+" "+val+"rows");
      int value=st.executeUpdate(sql);
       System.out.println(" table created successfully ");

//to close connection
connect.close();
//preparing statement
int cid =03;
String cname="IMS";
PreparedStatement st=connect.prepareStatement(sql);
st.setInt(1,cid);
st.setString(2,cname);
//execute query
      // ResultSet rs=st.executeQuery(sql);
          //while rs.next()){
        	//System.out.println(rs.getInt(1)+" "+rs.getSting(1) +" "+ rs.getString(2) +" "+getString(3));
        	
        }

      // rs.next();
      //System.out.println(rs.getInt(1)+" "+rs.getSting();



}
     }
